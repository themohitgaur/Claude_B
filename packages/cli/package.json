{
  "name": "claude-bootstrap",
  "version": "1.0.0",
  "description": "AI Workspace Generator for Claude Code — turn any repo into a configured Claude Code workspace in under a minute.",
  "type": "module",
  "bin": {
    "claude-bootstrap": "./bin/claude-bootstrap.js"
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": ["dist", "bin", "README.md"],
  "keywords": [
    "claude",
    "claude-code",
    "ai",
    "agents",
    "mcp",
    "subagents",
    "hooks",
    "skills",
    "agents.md",
    "scaffold",
    "cli",
    "bootstrap"
  ],
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts --clean",
    "dev": "tsup src/index.ts --format esm --dts --watch",
    "start": "node ./bin/claude-bootstrap.js",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@claude-bootstrap/scanner": "workspace:*",
    "@claude-bootstrap/generators": "workspace:*",
    "@claude-bootstrap/templates": "workspace:*",
    "commander": "^12.1.0",
    "inquirer": "^10.2.0",
    "chalk": "^5.3.0",
    "ora": "^8.1.0"
  },
  "devDependencies": {
    "tsup": "^8.3.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0",
    "@types/node": "^22.7.0",
    "@types/inquirer": "^9.0.7"
  },
  "engines": { "node": ">=18.0.0" },
  "license": "MIT",
  "repository": { "type": "git", "url": "https://github.com/your-org/claude-bootstrap" },
  "homepage": "https://github.com/your-org/claude-bootstrap#readme"
}
